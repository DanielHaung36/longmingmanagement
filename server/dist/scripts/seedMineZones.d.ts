#!/usr/bin/env ts-node
/**
 * seedMineZones.ts
 *
 * Loads default mine site records from prisma/seedData/mineZone.json
 * and upserts them into the database. Safe to run multiple times.
 *
 * Usage:
 *   npm run seed:mine-zones
 */
export {};
//# sourceMappingURL=seedMineZones.d.ts.map